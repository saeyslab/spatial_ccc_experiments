functionality:
  name: allen_brain_atlas_via_pooch
  arguments:
    - name: --output
      type: file
      description: Path to the output directory
      example: output.h5ad
    - name: --brain_section_label
      type: string
      description: Brain section label
      example: 'Zhuang-ABCA-1.080'
  resources:
    - type: python_script
      path: script.py
platforms:
  - type: docker
    image: python:3.10
    setup:
      - type: python
        packages:
          - pooch
          - anndata
          - pandas
          - scanpy
  - type: nextflow